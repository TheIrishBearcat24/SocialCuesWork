import { TextField, Autocomplete } from "@mui/material";
import React, { useState } from "react";

const socialCuesList: string[] = []

const Textbox = () => {
    const [getSocialCuesList, setSocialCuesList] = useState(socialCuesList)
}

const AutocompleteSearchBox: React.FC = () => {
    return null
}